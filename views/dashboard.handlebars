<button class="create-post">Create a new Post!</button>

{{#if posts.length}}
<section>
    <h2>Posts by you: </h2>
    <ol>
        {{#each posts as |post|}}
        <li>
            {{> post-info post}}
            <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit Post</a>
        </li>
        {{/each}}
    </ol>
</section>
{{/if}}

<script src="/js/nav-createPost.js"></script>